get:
  operationId: getEnterprise
  summary: Get your enteprise information
  description: Returns enterprise
  produces:
    - application/json
  parameters: []
  responses:
    '200':
      description: successful operation
      schema:
        $ref: '#/definitions/Enterprise'
  tags:
    - Enterprise
patch:
  operationId: partialUpdateEnterprise
  summary: Partial update enterprise information
  description: Returns updated enterprise
  produces:
    - application/json
  parameters:
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/Enterprise'
  responses:
    '200':
      description: successful operation
      schema:
        $ref: '#/definitions/Enterprise'
  tags:
    - Enterprise
parameters:
  - name: enterprise_id
    in: path
    description: A UUID string identifying this enterprise.
    required: true
    type: string
    format: uuid