post:
  operationId: lockDevice
  summary: Lock a device
  description: Creates DeviceCommand instance
  produces:
    - application/json
  parameters:
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/DeviceCommandRequest'
  responses:
    '201':
      description: successful operation
      schema:
        $ref: '#/definitions/DeviceCommand'
  tags:
    - v0
    - Device Commands
parameters: []
