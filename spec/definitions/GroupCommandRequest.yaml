required:
  - enterprise
  - group
type: object
properties:
  enterprise:
    title: Enterprise
    type: string
    format: uuid
  group:
    title: Group
    type: string
    format: uuid
  command_args:
    title: Command args
    type: string