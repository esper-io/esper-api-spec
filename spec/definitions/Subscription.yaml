type: object
properties:
  id:
    type: string
    format: uuid
    title: ID
    description: A UUID identifying this report delivery subscription.
    readOnly: true
  email_ids:
    type: array
    items:
      type: string
      format: email
    minItems: 1
    maxItems: 10
  is_subscribed:
    type: boolean
    title: Is subscribed
  created_on:
    type: string
    format: date-time
    title: Created on
    readOnly: true
  updated_on:
    type: string
    format: date-time
    title: Updated on
    readOnly: true
