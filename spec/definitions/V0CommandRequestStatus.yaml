type: object
properties:
  state:
    title: Command State
    description: State of
    type: string
    format: uuid

  total:
    title: Count
    description: No of commands currently in this  state
    type: integer
    format: int64