type: object
properties:
  reason:
    type: string
    description: details briefing the reason for current command state
  details:
    type: object
    properties:
      reason:
        type: string
        description: details briefing the reason for current command state